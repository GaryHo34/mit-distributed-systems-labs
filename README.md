## Developing workflow

### Fetch from upstream

```bash
$ git fetch upstream
$ git checkout master
$ git rebase upstream/master
```

### Create a new branch

```bash
$ git checkout -b <branch-name>
```

### Push to origin

```bash
$ git push origin <branch-name>
```
